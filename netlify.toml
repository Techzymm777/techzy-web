# All contexts (production, deploy previews, branch deploys) build and
# publish the Vite app. SPA routing fallback lives in public/_redirects.
# NOTE: this file overrides base/publish settings configured in the
# Netlify UI.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
